{
  "name": "dependify",
  "version": "1.0.0",
  "private": true,
  "description": "Dependify Business Operating System — AI-orchestrated platform for SMEs",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "db:push": "pnpm --filter @dependify/db push",
    "db:migrate": "pnpm --filter @dependify/db migrate",
    "db:studio": "pnpm --filter @dependify/db studio"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2",
    "prettier": "^3.4.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
